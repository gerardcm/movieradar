# Secrets / local config — never commit this, it holds your API keys and Telegram token
config.json

# Runtime state (which movies have already been notified about)
state.json

# run_from_github.py's runtime-fetched copy of the code — never the source of truth
_movieradar_fetched.py

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# OS cruft
.DS_Store
